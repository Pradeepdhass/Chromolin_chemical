import React from 'react';
import ProductCategory from '../ProductCategory';
import { productData } from '../../data/products';

const StabilizersSequestering = () => {
    return <ProductCategory {...productData.stabilizersSequestering} />;
};

export default StabilizersSequestering;
